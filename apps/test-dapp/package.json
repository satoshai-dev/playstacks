{
  "name": "test-dapp",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "playwright test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@stacks/transactions": "^7.3.0"
  },
  "devDependencies": {
    "@satoshai/playstacks": "workspace:*",
    "@playwright/test": "^1.50.0",
    "vite": "^6.3.0",
    "typescript": "^5.7.0"
  }
}
